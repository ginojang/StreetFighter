# → DragonHearts 핸드오프: 보간 · 이동 잔상(LCD sample-and-hold)

**보낸 곳:** StreetFighter (자매 프로젝트). DragonHearts의 고정틱+렌더보간 패턴을 도입하며 겪고 규명한 것들 중, DragonHearts에도 해당될 수 있는 findings를 넘긴다. (2026-09-02)

> 핵심 요약: **캐릭터 이동 시 "잔상/눈 아픔"은 코드 버그가 아니라 LCD sample-and-hold(디스플레이 특성)다.** DragonHearts도 동일 현상 확인됨. 코드로는 마스킹(모션블러)까지, 근본은 모니터(고주사율/BFI). 단 **1인 RPG인 DragonHearts는 "센터락"으로 상당히 완화 가능** — 이게 이 핸드오프의 실질 제안이다.

---

## 1. 진단: 이동 잔상 = LCD sample-and-hold (하드웨어)

- **증상:** 캐릭터가 화면상 이동할 때 잔상·이중상·눈피로. **화면 중앙(카메라가 캐릭터 추적=화면상 정지)이면 안 생기고, 맵 가장자리(스크롤 불가, 캐릭터가 화면상 이동)면 생긴다.** ← 이 "중앙 OK / 가장자리 발생" 패턴이 sample-and-hold의 결정적 증거다.
- **원리:** LCD는 한 프레임을 다음 refresh까지 붙잡는다(sample-and-hold). 눈이 움직이는 물체를 smooth-pursuit로 추적하면, 그 붙잡힌 정지 이미지가 망막을 가로질러 번진다. **CRT엔 없던 것**(임펄스 구동: 픽셀이 번쩍→꺼짐). 그래서 "예전 CRT가 더 부드러웠다"가 물리적으로 맞다.
- **왜 픽셀아트가 최악:** 딱딱한 고대비 픽셀 경계가 sample-and-hold로 번지면 뚜렷한 이중상이 된다. 3D가 괜찮아 보이는 건 안티에일리어싱+모션블러가 이를 숨기기 때문(면역이 아니라 마스킹).
- StreetFighter(바닐라 JS)와 DragonHearts(C++/WASM) 둘 다 동일 → **언어·엔진 무관, 디스플레이 특성 확정.**

## 2. ⭐ DragonHearts에 직접 유용: 센터락으로 맵 가장자리 완화

DragonHearts는 **1인 주인공 RPG** — StreetFighter(2인 격투)와 달리 **캐릭터를 화면 중앙에 고정(월드 스크롤)** 할 수 있다. 실제로 중앙에선 잔상이 없다. 문제는 **맵 가장자리에서 카메라가 더 못 스크롤해 캐릭터가 화면상 움직이는 구간**뿐이다.

**제안(엔진/카메라 검토):**
- 맵 경계에 **카메라 여백/데드존**을 둬, 가장자리에서도 캐릭터가 가급적 화면 중앙을 유지하게 한다(카메라를 미리 붙잡거나, 맵 밖 여백 픽셀 노출 허용).
- 그러면 가장자리에서도 캐릭터의 **화면상 이동량이 줄어** sample-and-hold 잔상이 완화된다.
- 트레이드오프: 맵 밖 여백이 보이거나(검은 여백/확장 배경), 카메라가 벽에 덜 붙는다. 디자인 결정 필요.

이게 픽셀아트 게임이 현대 LCD에서 잔상을 근본적으로 피하는 정석(캐릭터 센터락)이고, DragonHearts는 장르상 이걸 쓸 수 있다.

## 3. 코드 마스킹 옵션: 프레임 블렌딩(모션블러)

3D가 쓰는 트릭을 픽셀아트에 흉내낸 것. sample-and-hold를 **없애는 게 아니라 부드럽게 덮는다.**

- **방식(권장):** **비재귀 더블버퍼** — `display = a·(직전 클린 프레임) + (1-a)·(현재 클린 프레임)`. 직전 "클린"(블렌드 안 된) 1장만 섞는다.
- **함정 주의(StreetFighter가 겪음):** 재귀(EMA, 블렌드 결과를 다시 prev로)는 **어두워짐·이전 장면 잔류·밝기 전환 lag**를 유발한다. 반드시 비재귀로.
- 정지 화면은 prev≈현재라 선명 유지, 이동체만 번진다. 세기 a는 0.3~0.5가 자연스럽다(0.7↑은 이중상).
- StreetFighter는 이걸 `?blend=` 옵션(기본 0)으로 넣었다. **취향 편차 큼** — 우리 사용자는 결국 "블러 없는 선명함(보간 on, blur off)"을 선호했다. 옵션으로 두는 걸 권장.

## 4. ⚠️ 이건 DragonHearts엔 해당 없음 (시뮬 레이트 차이)

StreetFighter는 **시뮬 60fps**라 60Hz 디스플레이에서 display=sim → 보간 alpha가 rAF 타이밍 노이즈로 진동(지터)하는 문제가 있었고, **vsync 타임 스냅**(프레임 간격을 틱길이 정수배로 스냅)으로 고쳤다.

**DragonHearts는 시뮬 20fps** → 60Hz에서 display(60) > sim(20), 즉 틱당 3렌더프레임이라 **보간이 이득 구간이고 이 지터 문제가 없다.** `El_SetRenderPhase`로 비정수 케이던스(75/165Hz)도 이미 처리 중. 그러니 이 항목은 참고만.

## 5. 근본 해결

코드는 마스킹까지. **이동 잔상의 근본은 디스플레이** — 고주사율(120/144Hz, hold 시간↓)이나 모니터의 BFI/스트로빙(ULMB, 임펄스에 근접). 눈이 민감한 사용자에겐 이게 가장 효과적이다.

---

**액션 요청(있다면):** §2의 **맵 가장자리 카메라 센터락/데드존**이 DragonHearts에서 가장 실효성 있는 개선. 검토해보고 필요하면 진행. §3(모션블러 옵션)은 여유될 때. 나머지는 참고용.

*— StreetFighter 측 (2026-09-02). 원 패턴(고정틱+보간)을 준 게 DragonHearts라 되돌려주는 findings다.*
